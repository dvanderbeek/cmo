<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= @site.seo_title %><% if !@page.nil? && @page.seo_title != "" %> | <%= @page.seo_title %><% end %></title>
<meta content="<% if !@page.nil? && @page.seo_meta != "" %><%= @page.seo_meta %><% else %><%= @site.seo_meta %><% end %>" name="description">
<meta content="<% if !@page.nil? && @page.seo_keywords != "" %><%= @page.seo_keywords %><% else %><%= @site.seo_keywords %><% end %>" name="keywords">
<style>
body {
  padding-top: 60px;
}
.edit-buttons {
	position: fixed;
	bottom: 15px;
	right: 15px;
	background: #000;
	color: #fff;
	border-radius: 20px;
	-webkit-border-radius: 20px;
	-moz-border-radius: 20px;
}
.edit-buttons a {
	text-decoration: none;
	color: #FFF;
	padding: 10px 20px;
	display: block;
	border-radius: 20px;
}
</style>

<% @stylesheets.each do |stylesheet| %>
  <% if stylesheet.resource_url == '' %>
  	<link href="<%= stylesheet.resource.url %>" rel="stylesheet" type="text/css">
  <% else %>
  	<link href="<%= stylesheet.resource_url %>" rel="stylesheet" type="text/css">
  <% end %>
<% end %>

<link rel="shortcut icon" href="<%= @site.favicon.url(:icon) %>">
</head>

<%= liquidize @current_site.layout.content, {
	'page_content' => yield,
	'site' => @site,
	'page' => @page
	}
%>

<% @javascripts.each do |js| %>
	<% if js.resource_url == '' %>
		<script src="<%= js.resource.url %>" type="text/javascript"></script>
	<% else %>
		<script src="<%= js.resource_url %>" type="text/javascript"></script>
	<% end %>
<% end %>

</body>
</html>