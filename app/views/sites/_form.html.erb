<%= form_for(@site, :remote => true, :html => {:class => 'ctrl'}) do |f| %>
  <% if @site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site.errors.count, "error") %> prohibited this site from being saved:</h2>

      <ul>
      <% @site.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :title, :class => 'title' %>
  </div>
  <div class="field">
    <%= f.label :subdomain %>
    <%= f.text_field :subdomain %>
  </div>
  <div class="field">
    <%= f.label :seo_title %>
    <%= f.text_field :seo_title %>
  </div>
  <div class="field">
    <%= f.label :seo_meta %>
    <%= f.text_area :seo_meta, :rows => 5 %>
  </div>
  <div class="field">
    <%= f.label :seo_keywords %>
    <%= f.text_area :seo_keywords, :rows => 5 %>
  </div>
  <div class="field">
    <%= f.label :robots %>
    <%= f.text_area :robots, :rows => 5 %>
  </div>
  <div class="field">
    <%= f.collection_select(:layout_id, Layout.all, :id, :title)%>
  </div>
  <%= link_to "Edit Layout", edit_layout_url %>

  <div class="actions">
    <%= f.submit :class => "btn btn-success" %>
  </div>
<% end %>
